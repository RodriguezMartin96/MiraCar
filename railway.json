{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "cp .env.railway .env && composer install --no-dev && php artisan config:cache && php artisan route:cache && php artisan view:cache"
  },
  "deploy": {
    "startCommand": "php artisan serve --host=0.0.0.0 --port=$PORT",
    "healthcheckPath": "/",
    "healthcheckTimeout": 100,
    "restartPolicyType": "on_failure"
  },
  "nixpacks": {
    "php": "8.1"
  }
}